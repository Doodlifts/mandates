[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.26"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
# PoolManager exceeds EIP-170 without via_ir at low runs; raise the limit for local testing
code_size_limit = 150000
fuzz = { runs = 256 }
